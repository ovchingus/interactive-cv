/* If host on github pages and not as main page e.x. username.github.io
specify exact path from root.
To host in username.github.io/mypage option should be 'mypage' */

export const hostPath = 'interactive-cv'
